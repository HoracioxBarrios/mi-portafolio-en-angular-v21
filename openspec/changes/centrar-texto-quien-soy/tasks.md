## 1. Estilos del bloque "Quién soy"

- [x] 1.1 Convertir `.sobre-mi__header` en contenedor flex en columna con `align-items: center` y aplicar `text-align: center` solo a `.sobre-mi__title` en `src/app/features/sobre-mi/sobre-mi.scss`
- [x] 1.2 Confirmar que `.sobre-mi__title` queda centrado y `.sobre-mi__description` queda como bloque centrado con texto a la izquierda, conservando `max-width: 720px`

## 2. Verificación

- [x] 2.1 Ejecutar `npm run build` y comprobar que no hay errores de compilación
- [x] 2.2 Verificar en el navegador (dev server en el puerto 4200) que el título y la descripción de la card "Quién soy" aparecen centrados en desktop y en móvil